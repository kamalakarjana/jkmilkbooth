<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ supplier.name }} - Supplier Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .summary-card { border-radius: 10px; padding: 20px; margin-bottom: 20px; }
        .balance-positive { background: linear-gradient(135deg, #d4edda, #c3e6cb); }
        .balance-negative { background: linear-gradient(135deg, #f8d7da, #f5c6cb); }
        .table-responsive { overflow-x: auto; }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Milk Booth</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item"><a class="nav-link" href="/add_collection_page">Add Collection</a></li>
                    <li class="nav-item"><a class="nav-link" href="/daily">Daily Collections</a></li>
                    <li class="nav-item"><a class="nav-link" href="/daily_sales">Daily Sales</a></li>
                    <li class="nav-item"><a class="nav-link active" href="/suppliers">Suppliers</a></li>
                    <li class="nav-item"><a class="nav-link" href="/customers">Customers</a></li>
                    <li class="nav-item"><a class="nav-link" href="/sales">Sales</a></li>
                    <li class="nav-item"><a class="nav-link" href="/monthly">Monthly Report</a></li>
                    <li class="nav-item"><a class="nav-link" href="/withdrawals">Withdrawals</a></li>
                </ul>
                <ul class="navbar-nav">
                    <li class="nav-item"><a class="nav-link" href="/my_account">My Account</a></li>
                    <li class="nav-item"><a class="nav-link" href="/logout">Logout</a></li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <!-- Back Button -->
        <a href="/suppliers" class="btn btn-outline-secondary mb-3">← Back to Suppliers</a>
        
        <!-- Supplier Info Card -->
        <div class="card mb-4">
            <div class="card-header bg-primary text-white">
                <h4 class="mb-0">Supplier Information</h4>
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-md-6">
                        <table class="table table-borderless">
                            <tr>
                                <th width="40%">Supplier ID:</th>
                                <td><strong>{{ supplier.supplier_id }}</strong></td>
                            </tr>
                            <tr>
                                <th>Name:</th>
                                <td><strong>{{ supplier.name }}</strong></td>
                            </tr>
                            <tr>
                                <th>Mobile:</th>
                                <td>{{ supplier.mobile or 'Not provided' }}</td>
                            </tr>
                        </table>
                    </div>
                    <div class="col-md-6">
                        <table class="table table-borderless">
                            <tr>
                                <th width="40%">Address:</th>
                                <td>{{ supplier.address or 'Not provided' }}</td>
                            </tr>
                            <tr>
                                <th>Total Collections:</th>
                                <td>{{ collections|length }}</td>
                            </tr>
                            <tr>
                                <th>Account Created:</th>
                                <td>{{ supplier.created_at.strftime('%Y-%m-%d') if supplier.created_at else 'N/A' }}</td>
                            </tr>
                        </table>
                    </div>
                </div>
            </div>
        </div>

        <!-- Summary Cards -->
        <div class="row mb-4">
            <div class="col-md-3">
                <div class="card text-white bg-info">
                    <div class="card-body text-center">
                        <h5 class="card-title">Total Liters</h5>
                        <h2 class="card-text">{{ "%.2f"|format(total_liters) }} L</h2>
                    </div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="card text-white bg-success">
                    <div class="card-body text-center">
                        <h5 class="card-title">Total Amount</h5>
                        <h2 class="card-text">₹{{ total_amount }}</h2>
                    </div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="card text-white bg-warning">
                    <div class="card-body text-center">
                        <h5 class="card-title">Total Withdrawn</h5>
                        <h2 class="card-text">₹{{ total_withdrawn }}</h2>
                    </div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="card {% if balance >= 0 %}balance-positive{% else %}balance-negative{% endif %}">
                    <div class="card-body text-center">
                        <h5 class="card-title">Current Balance</h5>
                        <h2 class="card-text {% if balance >= 0 %}text-success{% else %}text-danger{% endif %}">
                            ₹{{ balance }}
                        </h2>
                        <small class="text-muted">
                            {% if balance >= 0 %}
                            (Supplier is owed this amount)
                            {% else %}
                            (Supplier owes this amount)
                            {% endif %}
                        </small>
                    </div>
                </div>
            </div>
        </div>

        <!-- Tabs for Collections and Withdrawals -->
        <ul class="nav nav-tabs mb-3" id="supplierTab" role="tablist">
            <li class="nav-item" role="presentation">
                <button class="nav-link active" id="collections-tab" data-bs-toggle="tab" data-bs-target="#collections" type="button">
                    Collections ({{ collections|length }})
                </button>
            </li>
            <li class="nav-item" role="presentation">
                <button class="nav-link" id="withdrawals-tab" data-bs-toggle="tab" data-bs-target="#withdrawals" type="button">
                    Withdrawals ({{ withdrawals|length }})
                </button>
            </li>
        </ul>

        <div class="tab-content" id="supplierTabContent">
            <!-- Collections Tab -->
            <div class="tab-pane fade show active" id="collections" role="tabpanel">
                <div class="card">
                    <div class="card-header">
                        <h5 class="mb-0">Milk Collections</h5>
                    </div>
                    <div class="card-body">
                        {% if collections %}
                        <div class="table-responsive">
                            <table class="table table-striped table-hover">
                                <thead>
                                    <tr>
                                        <th>#</th>
                                        <th>Date</th>
                                        <th>Session</th>
                                        <th>Liters</th>
                                        <th>Fat %</th>
                                        <th>Milk Type</th>
                                        <th>Rate/L</th>
                                        <th>Amount</th>
                                        <th>Note</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    {% for collection in collections %}
                                    <tr>
                                        <td>{{ loop.index }}</td>
                                        <td>{{ collection.date }}</td>
                                        <td>
                                            <span class="badge {% if collection.session == 'morning' %}bg-warning text-dark{% else %}bg-info{% endif %}">
                                                {{ collection.session|title }}
                                            </span>
                                        </td>
                                        <td>{{ "%.2f"|format(collection.liters) }}</td>
                                        <td>{{ "%.1f"|format(collection.fat) }}%</td>
                                        <td>{{ collection.milk_type|title }}</td>
                                        <td>₹{{ "%.2f"|format(collection.rate_per_liter) }}</td>
                                        <td><strong>₹{{ collection.amount }}</strong></td>
                                        <td>{{ collection.note or '-' }}</td>
                                    </tr>
                                    {% endfor %}
                                </tbody>
                                <tfoot>
                                    <tr class="table-secondary">
                                        <td colspan="3" class="text-end"><strong>Totals:</strong></td>
                                        <td><strong>{{ "%.2f"|format(total_liters) }} L</strong></td>
                                        <td></td>
                                        <td></td>
                                        <td></td>
                                        <td><strong>₹{{ total_amount }}</strong></td>
                                        <td></td>
                                    </tr>
                                </tfoot>
                            </table>
                        </div>
                        {% else %}
                        <div class="text-center py-5">
                            <h4 class="text-muted">No collections found</h4>
                            <p class="text-muted">This supplier hasn't supplied any milk yet.</p>
                        </div>
                        {% endif %}
                    </div>
                </div>
            </div>

            <!-- Withdrawals Tab -->
            <div class="tab-pane fade" id="withdrawals" role="tabpanel">
                <div class="card">
                    <div class="card-header">
                        <h5 class="mb-0">Payment Withdrawals</h5>
                    </div>
                    <div class="card-body">
                        {% if withdrawals %}
                        <div class="table-responsive">
                            <table class="table table-striped table-hover">
                                <thead>
                                    <tr>
                                        <th>#</th>
                                        <th>Date</th>
                                        <th>Amount</th>
                                        <th>Note</th>
                                        <th>Recorded On</th>
                                        {% if current_user.role == 'admin' %}
                                        <th>Actions</th>
                                        {% endif %}
                                    </tr>
                                </thead>
                                <tbody>
                                    {% for withdrawal in withdrawals %}
                                    <tr>
                                        <td>{{ loop.index }}</td>
                                        <td>{{ withdrawal.date }}</td>
                                        <td><strong class="text-danger">₹{{ withdrawal.amount }}</strong></td>
                                        <td>{{ withdrawal.note or '-' }}</td>
                                        <td>{{ withdrawal.created_at.strftime('%Y-%m-%d %H:%M') if withdrawal.created_at else 'N/A' }}</td>
                                        {% if current_user.role == 'admin' %}
                                        <td>
                                            <a href="{{ url_for('edit_withdrawal', wid=withdrawal.id) }}" class="btn btn-sm btn-outline-primary">Edit</a>
                                        </td>
                                        {% endif %}
                                    </tr>
                                    {% endfor %}
                                </tbody>
                                <tfoot>
                                    <tr class="table-secondary">
                                        <td colspan="2" class="text-end"><strong>Total Withdrawn:</strong></td>
                                        <td><strong class="text-danger">₹{{ total_withdrawn }}</strong></td>
                                        <td colspan="3"></td>
                                    </tr>
                                </tfoot>
                            </table>
                        </div>
                        {% else %}
                        <div class="text-center py-5">
                            <h4 class="text-muted">No withdrawals found</h4>
                            <p class="text-muted">No payments have been made to this supplier yet.</p>
                        </div>
                        {% endif %}
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
